{
  "name": "main-web-server",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "cross-fetch": "^3.1.5",
    "express": "^4.17.3"
  },
  "engines": {
    "node": "16.x"
  },
  "main": "index.js",
  "scripts": {
    "start": "node src/backend/app.js & node index.js",
    "build": "cd src/admin && npm install && npm run build && cd ../visualization && npm install && npm run build",
    "test": "cd src/admin && npm test && cd ../visualization && npm test"
  }
}