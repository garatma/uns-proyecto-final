{
  "name": "proyecto-final",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=16.x"
  },
  "scripts": {
    "start": "cd src/backend && npm start",
    "admin": "cd src/admin && npm start",
    "backend": "cd src/backend && npm start",
    "visualization": "cd src/visualization && npm start",
    "build": "cd src/admin && npm run build && cd ../visualization && npm run build",
    "install": "cd src/admin && npm install && cd ../backend && npm install && cd ../visualization && npm install",
    "test": "cd src/admin && npm test && cd ../visualization && npm test "
  },
  "devDependencies": {
    "eslint": "^8.14.0",
    "eslint-plugin-react": "^7.29.4",
    "prettier": "^2.6.2"
  },
  "dependencies": {
    "cron-time-generator": "^1.3.0",
    "node-schedule": "^2.1.0"
  }
}
